# Master Experiment Table

This table is the internal registry for all experiments in the BirdCLEF+ 2026 project.

| ID | Name | Status | Baseline | Model | Key Change | Local CV | Kaggle LB | Notes |
| --- | --- | --- | --- | --- | --- | --- | --- | --- |
| exp_001 | soundscape_reference_blend | completed | none | EfficientNet-B0 + GeM + attention SED head | Rebuild local validation around labeled soundscapes and compare `LB862`, `LB872`, `0.8 * LB872 + 0.2 * LB862`, and the same blend with notebook heuristics | pending | 0.890 | First Kaggle submission baseline validated on 2026-03-18 through `notebooks/kaggle_submission_reference_blend.ipynb`; local labels are still deduplicated by `(filename, start, end)` for future CV |
| exp_002 | train_audio_reproduction | completed | exp_001 | EfficientNet-B0 + GeM + attention SED head | Train the same architecture from scratch on isolated `train_audio` to replace borrowed checkpoints with a repository-native baseline | 0.9135 hold-out macro ROC-AUC at epoch 8 / 8 | 0.647 | Resume-from-checkpoint completed successfully on 2026-03-20; the pure single-checkpoint Kaggle submission underperformed badly versus local CV, confirming strong isolated-to-soundscape domain gap |
| exp_003 | perch_downstream_reproduction | completed | external Perch V2 starter | Frozen Perch V2 logits + embeddings + metadata priors + classwise logistic probes | Reproduce the downstream soundscape stack locally on cached `perch_meta` arrays without rerunning TensorFlow inference | 0.8353 OOF macro ROC-AUC on 59 fully labeled files (`0.7390 -> 0.8044 -> 0.8353`) | n/a | Confirms that most first-stage gain comes from texture-aware priors and that the probe adds another `+0.0309` on top of the OOF baseline; this is now the main soundscape-aware comparison target for `exp_004` |
| exp_004 | soundscape_finetuning | completed_first_run | exp_002 | EfficientNet-B0 + GeM + attention SED head | Finetune the native `exp_002` checkpoint on labeled `train_soundscapes` with grouped soundscape validation, soundscape background mixing, and a masked-BCE-compatible training loop | 0.7796 macro ROC-AUC at epoch 4 / 6 on the first grouped soundscape split | n/a | First run completed on 2026-03-21; validation only scored `29` classes because the single held-out soundscape fold is sparse, so this should be treated as a promising but noisy first native soundscape result rather than a final comparison to `exp_003` |
| exp_005 | native_priors_texture_postproc | completed | exp_004 | `exp_004` logits + metadata priors + texture-aware smoothing | Test whether `site/hour/site-hour` priors and texture smoothing improve the native soundscape model without retraining | 0.8157 macro ROC-AUC on the same fold (`0.7796 -> 0.8157`) | 0.737 | First Kaggle validation of the native hybrid confirms real transfer from local CV to public LB: the lightweight recipe improved substantially over pure `exp_002` (`0.647 -> 0.737`), but it still remains well below the stronger `0.890` reference blend, so inference-only adaptation is not enough yet |
| exp_006 | soundscape_finetuning_v2 | completed_first_3_folds | exp_004 | EfficientNet-B0 + GeM + attention SED head | Strengthen the native soundscape branch with non-zero `secondary_labels` weighting, a slightly larger replay bank, fold-specific outputs, and exported validation predictions for later OOF analysis | 0.7725 to 0.8313 macro ROC-AUC across folds 0-2, mean `0.7945` | n/a | Three folds are now available. The recipe is no longer a pure no-gain result, but it is still mixed: fold 1 was strong (`0.8313`), fold 0 matched the old baseline, and fold 2 dropped to `0.7725` while covering more classes (`35`). The export pipeline is working and the next question is whether priors on top of `exp_006` exports are stronger than the old `exp_004 + exp_005` branch |
| exp_007 | native_priors_on_exp006_oof | completed | exp_006 | `exp_006` exported OOF predictions + metadata priors + texture-aware smoothing | Apply the proven `exp_005` inference layer to fold-safe `exp_006` exports and compare raw-vs-postprocessed results on a more honest multi-fold OOF view | 0.7109 OOF macro ROC-AUC on folds 0-2 (`0.6646 -> 0.7109`); fold-mean best `0.8386` | 0.758 | Positive result both locally and on Kaggle. The 3-fold native hybrid beat the earlier native public result (`0.737 -> 0.758`), confirming that `exp_006 + priors` is a real improvement, but the uplift is still too small to challenge the `0.890` reference blend. This points to model/training changes, not more small postprocess tweaks, as the next main source of gains |
| exp_008 | long_context_native_sed | completed_first_3_folds | exp_007 | EfficientNet-B0 backbone + long-context temporal SED head | Replace independent `5s` clip classification with `20s` waveform contexts that emit `4` aligned `5s` windows and are aggregated back to row-level predictions with overlap-aware averaging | 0.8095 to 0.8377 macro ROC-AUC across folds 0-2, mean `0.8232` | n/a | The local branch now looks materially stronger and more stable than `exp_006` (`0.7945` mean), but the first single-fold leaderboard check through `exp_008b` still failed (`0.707`). Interpretation: long context is a real local modeling gain, yet current single-fold local validation is still too weak a proxy for promotion to the default Kaggle path |
| exp_008b | long_context_priors_postproc | completed | exp_008 | `exp_008` fold 0 exports + metadata priors + texture-aware smoothing | Apply the proven `exp_007` postprocessing layer on top of the first long-context fold to see whether the stronger native branch still benefits from priors | 0.8435 macro ROC-AUC on fold 0 (`0.8377 -> 0.8435`) | 0.707 | Negative leaderboard check. The local fold gain did not transfer: the first long-context submission underperformed both `exp_007` (`0.758`) and `exp_005` (`0.737`). The likely explanation is that the single sparse fold was not representative enough, so long-context should not become the default native submit path without more folds or a stronger validation protocol |
| exp_008c | long_context_priors_on_oof | completed | exp_008 | `exp_008` exported OOF predictions + metadata priors + texture-aware smoothing | Apply the `exp_008b` postprocessing recipe fold-safely across `exp_008` folds `0-2` and compare pooled OOF against the existing short-context native baseline | 0.7005 pooled OOF macro ROC-AUC on folds 0-2 (`0.6682 -> 0.7005`) | n/a | Important negative decision-point. Honest pooled OOF does not beat `exp_007` (`0.7109`), which explains why the single-fold long-context Kaggle attempt underperformed. `exp_008` remains a useful research branch, but it is not the next promoted submission path |
| exp_009 | noisy_student_pseudolabel | completed_3_folds_and_first_kaggle | exp_007 | EfficientNet-B0 + GeM + attention SED head student with `exp_006` fold teachers | Start the first repository-native noisy-student branch: generate fold-safe target-domain pseudo labels on unlabeled `train_soundscapes`, denoise them with power transform, and train a student with labeled soundscapes + replay + confidence-weighted pseudo sampling | fold `0`: `0.8495`; fold `1`: `0.8769`; fold `2`: `0.8849`; mean over folds `0-2`: `0.8704`; pseudo caches rows/files: `69593/9552`, `69640/9569`, `70142/9669` | 0.735 | Strong negative transfer result. Despite the best three-fold local picture among native branches, the first raw Kaggle submission underperformed both `exp_007` (`0.758`) and even the earlier postprocessed `exp_005` (`0.737`). The follow-up pooled OOF analysis in `exp_009c` confirmed that the branch remains useful research signal, but not yet a promoted submit path |
| exp_009b | noisy_student_priors_postproc | completed | exp_009 | `exp_009` fold `0` exports + metadata priors + texture-aware smoothing | Test whether the old native inference layer still helps after noisy-student training | raw remains best on fold `0`: `0.8495`; all prior variants regress (`0.7964` to `0.8112`) | n/a | Important positive-negative result: priors are no longer helping and instead hurt strongly. This suggests the noisy-student branch is learning more of the metadata/texture structure internally, so `exp_009` should be treated as a raw-model branch first rather than automatically inheriting `exp_007` postprocessing |
| exp_009c | noisy_student_pooled_oof_analysis | completed | exp_009 | `exp_009` fold exports + pooled OOF / calibration diagnostics | Build the honest pooled OOF view for `exp_009`, measure fold-mean optimism, and test whether lightweight context repair or temperature-style calibration rescues the branch before another Kaggle attempt | raw pooled OOF macro ROC-AUC `0.7934`; raw fold-mean `0.8704`; optimism gap `0.0770`; raw remains best fixed variant; fold-safe selection drops to `0.7853` | n/a | Critical analysis result. The branch is locally real, but its simple summary was overly optimistic. More importantly, no lightweight postprocess or calibration rescue improved pooled OOF over raw. That means the next move should be a new modeling branch, not another inference-only tweak on top of `exp_009` |
| exp_011 | hgnetv2_soundscape_supervised | completed_4_folds_and_second_kaggle | 0.856 HGNetV2 reference | `hgnetv2_b0.ssld_stage2_ft_in1k` | Rebuild the strong HGNet supervised recipe locally: unified `train_audio + labeled soundscape clips`, grouped multi-label CV, optional wav-cache, and soundscape-aware validation exports | folds `0-3`: soundscape-only macro ROC-AUC `0.8509 / 0.8042 / 0.8544 / 0.7992`, mean `0.8272`; overall macro ROC-AUC mean `0.9562` | 0.850 | Second Kaggle submission improved the branch again (`0.844 -> 0.850`). This validates the fold-expansion idea, but only with a modest gain, which suggests that `exp_011` is now a very strong native baseline with diminishing returns from more pure fold scaling. The next highest-value move is to compare this branch against `exp_012` rather than only adding more HGNet folds |
| exp_012 | perch_temporal_light | completed_first_oof_negative | 0.924 Pantanal Distill / ProtoSSM reference | `Perch v2 embeddings + ProtoTemporalLight (bi-SSM + prototypes + site/hour embeddings + gated fusion)` | Build a lighter first-pass temporal downstream model on cached `perch_meta` file sequences before attempting the full ProtoSSM stack | grouped OOF macro ROC-AUC `0.6248`; raw Perch on the same cached files `0.7390`; fold AUCs `0.8337 / 0.6862 / 0.8705` | n/a | Important negative result. The first light temporal model underperformed raw Perch by `-0.1142` on pooled OOF despite decent-looking per-fold scores, which means the current architecture is unstable and the fold summary is too optimistic. This branch is still valuable, but it now needs simplification/ablation before any Kaggle promotion |
| exp_012b | perch_temporal_ablation | completed_first_oof_negative | exp_012 | raw Perch + pooled MLP + simplified SSM variants | Build a cleaner ablation ladder over cached `perch_meta` to isolate whether the failure in `exp_012` comes from the temporal block itself or from the prototype/gated-fusion stack | grouped OOF macro ROC-AUC: `raw_perch 0.7390`, `pooled_mlp_rawfeat 0.6176`, `ssm_linear 0.3355`, `ssm_linear_rawfeat 0.4286` | n/a | Strong negative control. Raw Perch remained best and every learned variant regressed, which means the immediate problem is not only the prototype/gated-fusion complexity from `exp_012`. On the current trusted `59` files and grouped split, even much simpler Perch downstream models are not yet stable improvements. This sharply raises the value of the native `exp_011` path as the main optimization track |
| exp_014 | hgnetv2_pseudolabel | completed_4_folds_mixed_negative | exp_011 | `hgnetv2_b0.ssld_stage2_ft_in1k` student + `exp_011` 4-fold teachers | Continue the strongest native branch with fold-safe pseudo labels on unlabeled `train_soundscapes`, keeping the same HGNetV2 frontend/backbone and soundscape-aware checkpoint selection | folds `0-3`: soundscape-only macro ROC-AUC `0.8684 / 0.8154 / 0.8301 / 0.7702`, mean `0.8210`; pseudo caches `20688/5471`, `22188/5646`, `20211/5333`, `20595/5293` | n/a | Final four-fold result is negative relative to the supervised parent branch. `exp_014` wins on folds `0-1` but loses clearly on folds `2-3`, and the mean falls below `exp_011` (`0.8210` vs `0.8272`). This makes the branch valuable as a research finding, but not a promoted submit path |
| exp_014b | hgnetv2_pseudolabel_strict | completed_first_fold | exp_014 | `hgnetv2_b0.ssld_stage2_ft_in1k` student + `exp_011` 4-fold teachers | Test a stricter pseudo-label recipe: higher confidence threshold, lower pseudo weight, fewer pseudo windows per file, global pseudo cap, and delayed pseudo usage starting from epoch `2` | fold `0`: soundscape-only macro ROC-AUC `0.8683` at epoch `4 / 6`; pseudo cache `8724` rows across `3237` files with mean confidence `0.6358` | n/a | Strong diagnostic win. `exp_014b` matches `exp_014` on fold `0` almost exactly (`-0.00019`) while using a much smaller and cleaner pseudo cache. That does not yet make it a new submit path, but it strongly supports the hypothesis that `exp_014` failed because the pseudo recipe was too aggressive rather than because pseudo labels are useless |
| exp_015 | pantanal_proto_ssm_v17_submit_path | completed_first_kaggle | 0.924 Pantanal Distill / ProtoSSM reference | faithful external Perch + ProtoSSM v5 + probe + residual SSM submit stack | Operationalize the strongest known external notebook as a portable Kaggle submission path with dynamic input resolution instead of another local simplification | n/a | 0.925 | Major success. The faithful external operationalization not only worked, it slightly exceeded the reported `0.924` reference and became the new best public result in the repository. This strongly supports the conclusion from `exp_012` / `exp_012b`: the high external ceiling is real, but it must be preserved almost intact rather than approximated through light local Perch ablations |
| exp_015c | full_v18_submit_path | attempted_direct_submit_timeout | exp_015 + 0.927 Pantanal V18 reference | larger ProtoSSM + larger residual + updated probe/postprocess settings in one monolithic submit notebook | Test the full V18 upgrade as a direct Kaggle submit path without first artifactizing the downstream components | n/a | timeout | Useful negative engineering result. Even with `P100`, the notebook still timed out because submit mode continued to do heavy train-time work (`ProtoSSM`, probes, and sometimes residual correction training). This suggests the V18 path should be split into offline export and thin submit instead of being optimized further as a single notebook |
| exp_015d | v18_artifactized_submit_path | completed_first_kaggle | exp_015c | offline artifact export + thin Kaggle submit | Preserve the V18 quality hypothesis while removing submit-time training by precomputing and packaging downstream artifacts (`ProtoSSM`, probes, residual, priors, thresholds) | n/a | 0.929 | Major engineering and modeling win. Artifactization preserved the V18 stack, removed the submit-time timeout bottleneck, and improved the project best public score from `0.925` to `0.929`. This confirms that the V18 gains were real, and that the earlier failure mode was operational rather than conceptual |
| exp_016 | blend_exp015_exp011 | historical_scaffold_superseded | exp_015 + exp_011 | weighted blend of two existing `submission.csv` files | First complementarity scaffold after `exp_015 = 0.925`; later abandoned because Kaggle-downloaded CSV artifacts were not reliable enough for direct row-aligned blending | n/a | n/a | Useful historical branch. It established the blend question and produced the reusable runtime `exp_011` inference block, but the original CSV-based plan became obsolete once `exp_015d = 0.929` replaced `exp_015` as the main external path |
| exp_016b | blend_exp015d_exp011 | completed_first_kaggle_no_gain | exp_015d + exp_011 | artifactized V18 submit path plus runtime HGNetV2 blend | Test whether the strongest overall path (`exp_015d = 0.929`) still gains from a small runtime blend with the strongest repository-native branch (`exp_011 = 0.850`) without disturbing the timeout-safe artifactized V18 route | n/a | 0.929 | Clean no-gain result. After trimming the native add-on to the strongest `exp_011` folds and adding runtime guards, the blend matched `exp_015d` exactly instead of improving it. This suggests that simple generic complementarity from `exp_011` is weak at the current score level, so future blend work should be optional rather than a top priority |
| exp_015h | v18_timed_refresh_submit | completed_second_kaggle_negative | exp_015f | timed calibration-refresh thin submit with watchdogs and early snapshots | Convert the abandoned `exp_015f` line into a timer-aware submit path that can skip late stages and still finish with a valid `submission.csv` instead of timing out blindly | first Kaggle run timed out; later rerun completed and scored below baseline | 0.920 | Final negative result. The timed branch can be made to finish, but even after the runtime tweaks it underperforms the stable `exp_015d = 0.929` path by `-0.009`. This means the remaining `exp_015f/015h` calibration-refresh line is no longer an attractive promotion path for score, even aside from its engineering fragility |
| exp_017 | v18_error_report | completed | exp_015d and exp_011 diagnostics | pooled native error analysis notebook | Build a targeted diagnostic report by taxon, site, hour, and class difficulty so future improvements attack a concrete weak regime instead of another generic blend | pooled `exp_011` macro AUC `0.9468`; soundscape-only `0.7612`; weakest taxa `Amphibia`, then `Insecta` | n/a | Useful direction-setting branch. It showed clearly that the next modeling step should target texture-heavy taxa rather than add another generic ensemble |
| exp_018a | texture_specialist_oof | completed_4_folds_positive | exp_011 | `hgnetv2_b0.ssld_stage2_ft_in1k` specialist trained only on `Amphibia + Insecta` target rows | Test whether the native weakness on texture-heavy taxa is concentrated enough to justify a dedicated specialist model | target soundscape macro AUC over folds `0-3`: `0.8056 / 0.8254 / 0.7914 / 0.8004`; four-fold mean `0.8057`; same-taxa generic `exp_011` mean `0.7874` | n/a | Modest but real positive signal. The specialist is not stable enough to replace the generic branch globally, but it is strong enough to justify a correction-layer interpretation |
| exp_018b | targeted_merge_benchmark | completed | exp_018a + exp_011 | pooled aligned OOF targeted merge benchmark | Benchmark soft replacement of only `Amphibia + Insecta` columns to see whether a targeted correction branch beats both the generic and pure specialist extremes | generic target soundscape macro AUC `0.7218`; specialist `0.7450`; best tested targeted blend `w_spec = 0.75` gives `0.7465` | n/a | Strong local justification for a Kaggle overlay branch. The best result is a soft targeted merge, not a hard overwrite |
| exp_018c | exp015d_texture_overlay | scaffolded_for_first_kaggle | exp_015d + exp_018a | `exp_015d` V18 artifactized submit path with targeted HGNetV2 overlay for `Amphibia + Insecta` only | Keep the strongest external submit intact while blending in a conservative specialist correction only on the weak texture-heavy taxa | local design anchor from `exp_018b`: target-only best weight `0.75` on `exp_011` proxy; first Kaggle config intentionally reduced to `0.35` with folds `(0, 1)` | pending | First Kaggle-facing targeted overlay path. Packaged assets live in `submissions/kaggle_datasets/birdclef-exp018a-texture-specialist-4fold`, and the submit notebook is `notebooks/kaggle_submission_exp_018c_exp015d_texture_overlay.ipynb` |
| exp_018d | exp015d_texture_overlay_guarded | completed_first_kaggle_no_gain | exp_018c | `exp_015d` V18 artifactized submit path with timeout-safe single-fold HGNetV2 texture overlay | Convert the first timed-out overlay attempt into a guarded fallback path that preserves a valid pure-`exp_015d` submission if the specialist branch overruns | first public run used specialist fold `(1)` only, weight `0.25`, baseline snapshot before overlay, and explicit start/abort wall guards; final score matched baseline | 0.929 | Useful no-gain control. The guarded engineering path avoided the earlier timeout failure, but it did not improve the leaderboard over `exp_015d`. Without the saved runtime log we cannot cleanly distinguish “overlay skipped” from “overlay ran but was neutral,” yet the practical conclusion is the same: runtime texture overlay is not an immediate promotion path |
| exp_018e | exp015d_texture_overlay_accel | completed_first_kaggle_executed_neutral | exp_018d + exp_026a/026b | `exp_015d` V18 artifactized submit path with accelerated single-fold HGNetV2 texture overlay | Give the strongest runtime-blocked specialist idea one cleaner engineering rescue by adding backend selection `openvino -> torchscript -> eager`, optional prebuilt IR reuse, and explicit runtime logging | first Kaggle run completed at `0.929`; log confirms the specialist overlay actually ran with `runtime_backend = torchscript`, `active = true`, `elapsed_seconds ≈ 18.9`, and baseline/after-overlay snapshots were both written | 0.929 | Strong neutral result. This closes the ambiguity left by `exp_018d`: the specialist overlay was not merely skipped by guards. It executed successfully under TorchScript on CPU and still failed to improve the public leaderboard, so the current deployable overlay form should be considered genuinely neutral rather than just runtime-blocked |
| exp_019 | v18_postproc_ablation | completed_first_proxy_run | exp_015d | fixed-artifact V18 replay plus low-risk postprocess sweep | Benchmark cheap postprocess changes on top of the strongest artifactized V18 path before spending more Kaggle submissions on heavier branches | best proxy row-level variant: `no_file_scale` (`0.993906` vs baseline `0.993120`, `+0.000786`); safer cheap candidate: `no_rank_aware` (`0.993606`) with unchanged file-level AUC | n/a | Useful direction-setting proxy result. The first run suggests the current V18 postprocess may be slightly overbuilt, but the strongest row-level winner (`no_file_scale`) hurts file-level AUC, so the safest first public patch is likely `no_rank_aware` rather than the raw top proxy row winner |
| exp_019a | exp015d_no_rank_aware_submit | completed_first_kaggle_negative | exp_019 + exp_015d | thin `exp_015d` submit patch with rank-aware scaling disabled | Publicly test the safest cheap `exp_019` proxy winner without changing the underlying V18 artifact stack | only postprocess change: force `CFG["rank_aware_scale"] = False` after artifact load; first Kaggle run underperformed baseline | 0.928 | Useful thin negative control. The local proxy hint did not transfer to the public leaderboard, and the result is slightly worse than `exp_015d = 0.929`, so rank-aware scaling should remain part of the stable V18 recipe |
| exp_019b | exp015d_no_file_scale_submit | completed_second_kaggle_timeout_closed | exp_019 + exp_015d | thin `exp_015d` submit patch with file-level confidence scaling disabled | Publicly test the highest-upside but higher-risk `exp_019` proxy winner without changing the underlying V18 artifact stack | only postprocess change: force `CFG["file_level_top_k"] = 0` after artifact load; both Kaggle attempts timed out | timeout | Useful operational negative result. Because the patch changes only a late postprocess step, the repeated timeout is more informative about deployment fragility than about model quality. After two retries, this cheap public line is not worth more submissions |
| exp_020a | texture_artifact_correction_oof | completed_local_negative_proxy | exp_015d + exp_018b ideas | fixed-artifact V18 replay plus lightweight per-class texture correction models | Replace the failed runtime specialist overlay idea with a thin artifactized correction layer only for `Amphibia + Insecta` | grouped OOF correction benchmark on trusted full rows; all positive correction weights degraded the baseline and best blend weight stayed at `0.00` | n/a | Useful negative control. The target proxy was already near-saturated on trusted full rows (`0.997988` row AUC baseline), so this exact benchmark did not validate the thin texture-correction idea. If revisited, the correction branch should be redesigned around soundscape-aware / grouped file-level targets instead of this full-row proxy |
| exp_020b | texture_soundscape_file_correction_oof | completed_local_negative_proxy | exp_020a + exp_015d | fixed-artifact V18 replay plus lightweight per-class texture correction models on grouped file targets | Redesign the failed `exp_020a` proxy so the correction branch is selected on soundscape file-level metrics instead of saturated trusted-row AUC | file-level target benchmark still stayed negative: all positive correction weights underperformed baseline, best blend weight stayed at `0.00`, and only `10/42` target labels had enough support to train | n/a | Strong negative control. This is much more informative than `exp_020a`: even after moving to the more realistic grouped file-level proxy, the thin texture-correction layer still failed. The current deployable-form specialist correction line should therefore stay closed unless we later change the supervision source or target design substantially |
| exp_021 | v18_multishift_tta_benchmark | completed_local_near_neutral | exp_015d + monolithic V18 reference ideas | fixed-artifact V18 replay with controlled temporal-shift TTA variants | Benchmark whether the only still-plausible active difference from the monolithic `0.927-0.930` references, broader multi-shift TTA, transfers to our cleaner `exp_015d` stack | best file-level variant was `(0,1)`, but it only moved file macro AUC by about `+0.000019` while row macro AUC dropped by about `-0.000079`; wider TTA sets were neutral or slightly worse | n/a | Useful near-neutral control. The repeated multi-shift TTA idea from the monolithic references does not show a convincing local win on top of `exp_015d`, so it should not be a default next public patch unless we explicitly want one final cheap confirmation |
| exp_022 | hgnetv2_oof_pseudodistill_agreement | completed_fold0_negative_schedule | exp_014b + exp_011 + pseudo/distill research notes | stricter HGNetV2 OOF pseudo/distillation branch with teacher-agreement filtering | Revisit the strongest remaining native research direction after the thin patch lines closed, but do it with a much stricter pseudo recipe than `exp_014/014b` | fold `0` produced a very clean pseudo cache (`4895` rows, mean confidence `0.651`, agreement mean `0.993`), but the best epoch was still the last `labeled_only` epoch; once pseudo started, soundscape AUC declined | n/a | Strong research result but weak practical one. The branch shows that pseudo noise is no longer the main bottleneck; the current problem is the training schedule itself. This exact recipe should not be promoted to more folds without a materially different pseudo introduction strategy |
| exp_023a | v18x_dmodel384_artifact_export | completed_local_negative | exp_015c + `pantanal-distill-birdclef2026-v18x-dmodel-0.929` | artifactized V18x export branch with larger `ProtoSSM` width | Test the only clearly new active modeling ingredient from the `v18x` reference on top of the cleaner repository export scaffold: `ProtoSSM d_model 320 -> 384`, while keeping the same downstream artifactized shape | `oof_auc_proto = 0.6535`; `ensemble_auc = 0.9617`; `ensemble_weight = 0.0`; residual skipped | n/a | Strong negative local result. The larger `ProtoSSM` trained successfully but contributed so little useful OOF signal that the downstream fusion collapsed to pure probe/base behavior (`ensemble_weight = 0.0`). This does not justify a matching thin submit branch |
| exp_024 | geo_regime_ablation | completed_local_negative | exp_015d + exp_017 weak-regime diagnostics | fixed-artifact V18 replay plus controlled `site/hour` weak-regime ablations | Check whether the already-strong `exp_015d` stack still leaves meaningful geo signal in the weakest sites/hours, especially for `Amphibia/Insecta`, before trying any geo-specific submit patch | baseline won both overall and weak-regime file AUC; best variant stayed `manifest_baseline`; geo-only LR trained `36` classes but improved only `1` and worsened `28` | n/a | Strong negative control. In deployable form, the geo-regime line appears exhausted: neither weak-site/hour boosts nor cheap geo-only correction improved the already metadata-rich `exp_015d` path |
| exp_025 | hgnetv2_curriculum_pseudodistill | completed_fold0_negative_schedule | exp_022 + exp_011 | schedule-first HGNetV2 pseudo/distillation branch with ranked pseudo curriculum | Revisit the strongest remaining native pseudo idea, but change only how pseudo supervision is introduced: later start, lower initial weight, and gradual expansion from the top-ranked pseudo subset | fold `0` matched the same best labeled-only checkpoint as `exp_022` (`0.851456`) and still declined after pseudo activation despite the delayed curriculum | n/a | Strong follow-up negative result. This is more informative than `exp_022` alone: even after delaying pseudo start and ramping both weight and pool size, the best epoch still stayed in the labeled-only phase. That makes the current HGNetV2 pseudo line much harder to justify without a more substantial redesign |
| exp_026a | torchscript_hgnet_benchmark | completed_local_modest_gain | exp_011 + forum inference-speed discussion | local CPU benchmark for eager Torch vs `torch.jit.trace` on HGNetV2-B0 | Measure how much of the reported CPU inference gain transfers to our own native HGNet checkpoint before considering any Kaggle-facing inference rewrite | best eager: `45.845s` at `8` threads; best traced: `44.941s` at `8` threads; practical speedup about `1.97%`; same ordering held across all tested thread counts | n/a | Useful but modest engineering result. TorchScript consistently helps on our own checkpoint, but the gain is far smaller than the dramatic forum OpenVINO wins, so this alone does not justify a broad deployment rewrite unless a future native submit path becomes CPU-bound |
| exp_026b | openvino_hgnet_benchmark | completed_partial_without_openvino | exp_026a + forum inference-speed discussion | local CPU benchmark for eager Torch, TorchScript, and optional ONNX/OpenVINO sync/async HGNet inference | Test the full `Torch -> ONNX -> OpenVINO IR -> compile_model -> AsyncInferQueue` pipeline on our own native checkpoint, while degrading gracefully when conversion/runtime deps are missing | because local `.venv` still lacks `onnx`, `onnxruntime`, and `openvino`, the first run only repeated the Torch/TorchScript benchmark; results matched `exp_026a` closely and again showed only a small TorchScript gain | n/a | Useful engineering confirmation but not yet an OpenVINO result. The graceful-fallback path works, but we still do not know whether OpenVINO gives a worthwhile speedup or acceptable numerical drift on our own checkpoint |
| exp_027a | exp015d_teacher_cache | scaffolded | exp_015d | fixed V18 artifact replay exported as a fold-aware teacher cache on fully labeled soundscape rows | Turn the strongest external public path into a reusable local teacher by replaying `exp_015d` on trusted soundscape rows and saving aligned logits / probabilities / labels / fold assignments | pending | n/a | This is the first step of the next high-upside branch: instead of more tweaks around `exp_015d`, use it as a target-domain teacher for a genuinely different native student family |
| exp_027b | hgnetv2_soundscape_distill_from_exp015d | completed_fold0_negative_relative_to_teacher | exp_027a + exp_011 | soundscape-only HGNetV2 student distilled from fixed `exp_015d` teacher targets | Start a native student from the best supervised HGNet fold checkpoint and train it on fully labeled soundscape rows with both ground-truth BCE and teacher-target distillation | fold `0`: best soundscape macro AUC `0.961055` on `43` scored classes and `168` valid rows, but the aligned teacher remains far stronger on the same rows at about `0.996472`; best local blend stays at pure teacher | n/a | Strong negative relative result. The student is good in absolute terms on the trusted subset, but not close enough to the teacher to create local blend upside. This makes the direct teacher-student branch much weaker than it first appears from the raw AUC alone |
| exp_027c | exp015d_native_student_blend_benchmark | scaffolded | exp_027a + exp_027b | local soundscape OOF-style blend benchmark between the fixed V18 teacher and the distilled native student | Check whether the new student adds complementary signal over `exp_015d` before spending Kaggle attempts on any new submission path | pending | n/a | If this benchmark stays flat, the branch is probably not worth promoting; if it rises, it becomes the cleanest candidate for the next real post-`0.929` submit attempt |
| exp_0280 | clap_cache_build | completed_local_success | exp_027a | trusted-row CLAP cache build | Build a `row_id`-aligned CLAP embedding cache for the same trusted soundscape rows used by `exp_027a`, so that `exp_028a` can test a genuinely new external family without any more format work | built `708` aligned rows across `59` files; embedding dim `512`; `row_id_match = true`; local build completed quickly on `mps` | n/a | Clean infrastructure success. The CLAP branch is no longer blocked by missing embeddings: the cache contract now matches `exp_028a` exactly, so the next step can focus purely on complementarity rather than format/debug work |
| exp_028a | clap_perch_complementarity_benchmark | completed_local_strong_negative | exp_027a + exp_0280 | CLAP embedding probe OOF plus late blend against the fixed `exp_015d` teacher cache | Test whether a genuinely new external family can add complementary local signal on the same trusted rows where the current teacher-student line failed | teacher macro AUC `0.993120`; CLAP macro AUC `0.477363`; teacher texture macro AUC `0.997988`; CLAP texture macro AUC `0.543079`; best blend stays at pure teacher with `best_weight_clap = 0.0` | n/a | Strong scouting negative. This is not a close call or a calibration-only miss: even a tiny CLAP blend degrades both overall and texture metrics immediately. The current CLAP line should be considered closed before any larger integration effort |
| exp_029a | perch_onnx_compat_benchmark | scaffolded | exp_015d + exp_027a | strict compatibility benchmark for official Perch cache vs ONNX-exported Perch cache under fixed `exp_015d` replay | Test whether replacing the current official/TensorFlow Perch inputs with an ONNX-exported Perch cache is safe for the artifactized `exp_015d` stack before spending time on runtime-only deployment changes | pending | n/a | This is a safety check, not a score branch. It measures both raw cache drift and downstream replay deltas on the trusted `exp_027a` rows; ONNX is only worth pursuing further if those deltas stay negligible |
| exp_029b | exp015d_runtime_port | completed_first_kaggle_timeout | exp_015d + `bird26-reproduce-perch-protossm-resssm-inf-train.ipynb` | submit-facing runtime port of the fixed `exp_015d` scaffold | Test whether ONNX Perch, async prefetch, vectorized MLP probes, and batched TTA can reduce runtime pressure on the strongest public path without changing the actual V18 score recipe | first Kaggle run timed out; later notebook audit showed the branch still allowed a quiet fallback into the slower TensorFlow Perch path because `onnxruntime` was not installed from the attached ONNX dataset | timeout | Useful engineering miss, not a modeling miss. The result was valuable because it isolated the main weakness of the first runtime port: the notebook was not truly ONNX-first, so timeout here did not yet invalidate the ONNX idea itself |
| exp_029c | exp015d_onnx_first_runtime_port | completed_first_kaggle_runtime_win | exp_029b + local ONNX dataset audit | stricter ONNX-first submit-facing runtime port of the fixed `exp_015d` scaffold | Prevent the `exp_029b` failure mode where missing `onnxruntime` quietly forces a slow TensorFlow path: explicitly install `onnxruntime` from the attached dataset, resolve ONNX labels directly, and fail early if ONNX cannot activate | first Kaggle run completed in about `23` minutes and matched the stable `exp_015d` public score exactly | 0.929 | Major engineering win. This is the first branch to preserve the full `exp_015d` public score while dramatically reducing runtime pressure. Even without a score gain, it meaningfully expands what is operationally possible on top of the V18 path and is now the strongest deployment-oriented version of the current leaderboard recipe |
| exp_030 | exp029c_texture_overlay_multifold | completed_first_kaggle_strong_negative | exp_029c + exp_018a/018e | ONNX-first V18 base plus stronger multi-fold texture specialist overlay | Re-test the strongest remaining specialist idea now that runtime headroom is no longer the main blocker: use the fast `exp_029c` scaffold and blend several positive `exp_018a` folds instead of the earlier single-fold conservative overlay | first Kaggle run used folds `(0,1,3)` with weight `0.35`; ONNX base and torchscript overlay both activated cleanly; overlay finished quickly but public score collapsed well below baseline | 0.920 | Strong closing negative result. This is more informative than the earlier neutral single-fold overlay tests: the stronger deployable specialist form is not merely neutral, it is actively harmful on the public leaderboard. The texture overlay line should now be considered closed in submit-facing form |
| exp_031 | overlap_aware_soundscape_supervision | reinterpreted_after_ablation_not_overlap_driven | exp_027a + exp_027b + exp_011 | overlap-aware HGNetV2 supervision on trusted soundscape files with synthetic `2.5s`-hop windows | Test whether the current native line is bottlenecked by fixed non-overlapping `5s` supervision rather than by architecture alone: add overlap windows, interpolate soft targets from adjacent trusted rows, and keep validation on the original holdout rows | initial fold `0` run reached `0.961173` on `168` valid rows, but the follow-up ablation showed that `base_plus_overlap` was not actually the best regime | n/a | Important reinterpretation. The original tiny gain should not be credited to overlap-aware supervision itself. After the controlled ablation, the overlap branch no longer looks causally supported as the source of improvement |
| exp_031b | overlap_ablation | completed_fold0_overlap_hypothesis_negative | exp_031 + exp_027b | controlled ablation of `base_only`, `base_plus_overlap`, and `overlap_only` on the same trusted fold | Decide whether the tiny `exp_031` gain is actually caused by overlap-aware supervision or just by changing train-set size / composition | fold `0`: `base_only = 0.961637`, `overlap_only = 0.960023`, `base_plus_overlap = 0.958346`; best regime is `base_only` | n/a | Strong causal clarification. The overlap idea itself is not helping here: adding overlap rows hurts relative to the matched `base_only` regime. If there is any live native-side signal in this neighborhood, it now looks more like a lighter base-only recipe than an overlap-supervision effect |
| exp_031c | base_only_recipe_ablation | completed_folds01_mixed_no_robust_signal | exp_031b + exp_027b | controlled ablation of the old `exp_027b`-style base recipe versus the lighter `exp_031`-style base-only recipe | Check whether there is still a small native-side upside after removing overlap entirely, or whether the whole post-`exp_027b` branch is effectively exhausted | fold `0`: `light_base_recipe = 0.961107`, `legacy_base_recipe = 0.961055`; fold `1`: `legacy_base_recipe = 0.956123`, `light_base_recipe = 0.955171`; winner flips across folds | n/a | Mixed result with no robust winner. The lighter recipe does not hold up across folds, so this native follow-up line no longer looks reliable enough to prioritize. Any remaining gain here appears too small and unstable to matter for the main leaderboard path |
| exp_032 | top_solution_delta_audit | completed_local_audit | exp_029c + top local reference notebooks | reference-family audit across local `0.928-0.940` solutions | Separate genuinely new model families from code-near V18 / ProtoSSM variants before opening another research branch | core V18 references remain tightly clustered with overlap about `0.884-0.891`; `v18x` is only moderately different at `0.543`; the clearly distinct families are `smart-audio-bird-detector` (`0.068`) and the Perch-probe pair (`0.235-0.249`) | n/a | Important roadmap reset. Most top-looking local notebooks are still the same Perch + ProtoSSM + Residual family already optimized into `exp_029c`. The most promising genuinely new scouting target is now the `smart-audio-bird-detector` family, while the Perch-probe pair looks more like a lower-upside adjacent family |
| exp_033 | smart_audio_family_benchmark | completed_local_strong_positive_new_family | exp_032 + exp_027a + local smart-audio checkpoints | trusted-row benchmark for the `birdclef-2026-smart-audio-bird-detector` family | Replay the distinct EfficientNet-style smart-audio family on the same trusted `exp_027a` rows, compare its internal baseline/finetuned/blended variants, and test late `teacher + smart-audio` blends before any Kaggle promotion | `finetuned_only = 0.996936` vs teacher `0.993120`; best late blend stays at pure smart with `best_weight_smart = 1.0`; texture also improves (`0.998080` vs `0.997365`) | n/a | Very strong scouting result. Unlike recent branches, this is not merely a small complementary bump: the best smart-audio variant itself beats the fixed `exp_015d` teacher on trusted rows. The next logical step is now a pure smart-audio Kaggle benchmark rather than another late-blend experiment |
| exp_034 | smart_audio_submit_benchmark | completed_public_negative | exp_033 + local smart-audio checkpoints | first Kaggle-facing benchmark for the smart-audio family | Port the locally winning `finetuned_only` smart-audio path into a clean submit notebook that resolves model weights automatically, builds its inference manifest from `sample_submission.csv`, and writes runtime audit logs | local trusted-row scout was strongly positive, but first public submit scored far below the production path | 0.887 | Strong public negative. The smart-audio family looked excellent on trusted rows, but the pure submit did not transfer to public LB and underperformed `exp_029c = 0.929` by `-0.042`. Keep the notebook as a diagnostic artifact, but do not prioritize more smart-audio submit work without a clear domain-shift explanation |
| exp_035 | pantanal_onnx093_replay | completed_public_negative | `pantanal-distill-birdclef2026-onnx-0.93` + `bird26` ONNX runtime ideas | Pantanal `0.930` V18/ProtoSSM replay with strict real ONNX Perch and audio prefetch | Reproduce the newly added `0.930` Pantanal reference while correcting its misleading ONNX claim: keep the submit-time ProtoSSM/ResidualSSM recipe close to the reference, but force actual `onnxruntime` Perch inference and log backend activation | first public run underperformed the stable path, suggesting ONNX drift or implementation deltas changed the score recipe | 0.926 | Public negative for the ONNX port. This does not yet invalidate the Pantanal `0.930` recipe itself, because the reference notebook is actually TensorFlow-based; it only says our strict real-ONNX replay is not score-equivalent enough in this form |
| exp_036 | pantanal_tf_exact_replay | scaffolded_for_first_kaggle | `pantanal-distill-birdclef2026-onnx-0.93` exact code | exact TensorFlow replay of the Pantanal `0.930` reference | Remove our ONNX/runtime modifications and run the reference code path itself with the current Kaggle inputs, to test whether we can reproduce the reported `0.930` before optimizing anything | notebook scaffolded as code-identical to the source reference with saved outputs cleared | pending | This is the correct next control after `exp_035 = 0.926`: first recover the reference score with the exact TF recipe, then only change one engineering detail at a time |
| exp_037 | pantanal_onnx_tf_aligned | completed_public_negative | exp_036 + ONNX Perch | minimal ONNX test-inference patch with ONNX-to-TF feature alignment | Try to recover ONNX runtime speed without losing the TensorFlow reference score by keeping the Pantanal train recipe unchanged, using ONNX only for Perch inference, and calibrating ONNX logits/embeddings back to the cached TF Perch feature space on `perch_meta` rows | alignment did not recover the public score; result matched the earlier strict ONNX port | 0.926 | Strong ONNX-path negative. Since both raw strict ONNX and ONNX-to-TF aligned variants landed at `0.926`, the current ONNX acceleration route is not score-safe for this Pantanal downstream stack. Pause ONNX optimization until the exact TF replay establishes the reference score |
| exp_038 | pantanal_onnx_fast_noalign | completed_public_runtime_win | exp_036 + `bird26` ONNX runtime ideas | Pantanal reference recipe with real ONNX Perch, async audio prefetch, and batched TTA but no ONNX-to-TF alignment | Remove the likely timeout source from the last ONNX attempt by skipping the extra full-cache alignment pass while keeping the two low-risk runtime optimizations from the fast public baseline | first fixed Kaggle run completed and returned to the stable public plateau | 0.929 | Important engineering recovery. The rank-adapted ONNX input plus resolved `perch_meta` cache fixed the runtime/compatibility issue and restored the stable score, but it still did not break the `0.929` ceiling. Treat this as a safe fast base, not as a score-improving branch |
| exp_039 | pantanal_onnx_fast_no_file_scale | completed_public_negative | exp_038 + exp_019b | fast `exp_038` base with file-level confidence scaling disabled | Re-test the strongest `exp_019` proxy winner now that the timeout issue is solved by the ONNX fast base; change only `CFG["file_level_top_k"] = 0` | public run completed and strongly underperformed the stable base | 0.922 | Strong negative. The earlier `exp_019b` timeout ambiguity is now resolved: disabling file-level confidence scaling is harmful on public hidden data, despite the trusted-row proxy row-AUC signal. Keep file-level scaling enabled in the stable `exp_038` recipe |
| exp_040 | v18_strict_filelevel_proxy_audit | completed_local_no_candidate | exp_019 + public negatives | stricter file-level/regime-aware postprocess proxy audit | Re-run cheap V18 postprocess candidates with selection based on file macro AUC, texture/event file AUC, taxon file AUC, and worst-regime deltas instead of row macro AUC | baseline remained best by strict file score; no variant passed public-candidate gates | n/a | Strong guardrail result. Global postprocess deletion/tuning should stop: `no_rank_aware` and `no_file_scale` only looked good on row-level proxy, while file/regime metrics either stayed flat or degraded. Keep `exp_038` as baseline; only revisit postprocess via targeted taxon-aware variants |
| exp_041 | v18_texture_targeted_graft_audit | completed_local_candidate | exp_040 + exp_039 | texture-only postprocess column graft audit | Test whether the small texture-file gain from `no_file_scale/topk1` can be grafted only onto `Amphibia + Insecta` columns while keeping Aves/event columns at baseline | best strict candidate `texture_graft_no_file_scale_w100`: file macro `0.9923046` vs baseline `0.9921255` (`+0.0001791`); texture file `+0.000303`; event file unchanged; worst file-regime delta `0.0` | n/a | First postprocess proxy since the public negatives that passes the strict gate. This is not the global `no_file_scale` patch that scored `0.922`; it is a targeted texture-only graft that leaves Aves/event columns unchanged. Promote to a thin `exp_038` submit patch |
| exp_042 | v18_texture_graft_no_file_scale | completed_public_negative | exp_038 + exp_041 | thin submit patch with texture-only `no_file_scale` graft | Keep the stable V18/ONNX-fast submit stack unchanged, build baseline probabilities and a donor postprocess with `file_level_top_k = 0`, then replace only `Amphibia + Insecta` columns | strict local proxy candidate underperformed public baseline | 0.926 | Strong postprocess warning. The targeted graft reduced the global `no_file_scale` damage (`exp_039 = 0.922 -> 0.926`) but still lost to the stable `0.929` base. This closes the current file-scale/rank/top-k postprocess line unless a new validation protocol explains the public mismatch |

# Master Experiment Table

This table is the internal registry for all experiments in the BirdCLEF+ 2026 project.

| ID | Name | Status | Baseline | Model | Key Change | Local CV | Kaggle LB | Notes |
| --- | --- | --- | --- | --- | --- | --- | --- | --- |
| exp_001 | soundscape_reference_blend | completed | none | EfficientNet-B0 + GeM + attention SED head | Rebuild local validation around labeled soundscapes and compare `LB862`, `LB872`, `0.8 * LB872 + 0.2 * LB862`, and the same blend with notebook heuristics | pending | 0.890 | First Kaggle submission baseline validated on 2026-03-18 through `notebooks/kaggle_submission_reference_blend.ipynb`; local labels are still deduplicated by `(filename, start, end)` for future CV |
| exp_002 | train_audio_reproduction | completed | exp_001 | EfficientNet-B0 + GeM + attention SED head | Train the same architecture from scratch on isolated `train_audio` to replace borrowed checkpoints with a repository-native baseline | 0.9135 hold-out macro ROC-AUC at epoch 8 / 8 | 0.647 | Resume-from-checkpoint completed successfully on 2026-03-20; the pure single-checkpoint Kaggle submission underperformed badly versus local CV, confirming strong isolated-to-soundscape domain gap |
| exp_003 | perch_downstream_reproduction | completed | external Perch V2 starter | Frozen Perch V2 logits + embeddings + metadata priors + classwise logistic probes | Reproduce the downstream soundscape stack locally on cached `perch_meta` arrays without rerunning TensorFlow inference | 0.8353 OOF macro ROC-AUC on 59 fully labeled files (`0.7390 -> 0.8044 -> 0.8353`) | n/a | Confirms that most first-stage gain comes from texture-aware priors and that the probe adds another `+0.0309` on top of the OOF baseline; this is now the main soundscape-aware comparison target for `exp_004` |
| exp_004 | soundscape_finetuning | completed_first_run | exp_002 | EfficientNet-B0 + GeM + attention SED head | Finetune the native `exp_002` checkpoint on labeled `train_soundscapes` with grouped soundscape validation, soundscape background mixing, and a masked-BCE-compatible training loop | 0.7796 macro ROC-AUC at epoch 4 / 6 on the first grouped soundscape split | n/a | First run completed on 2026-03-21; validation only scored `29` classes because the single held-out soundscape fold is sparse, so this should be treated as a promising but noisy first native soundscape result rather than a final comparison to `exp_003` |
| exp_005 | native_priors_texture_postproc | completed | exp_004 | `exp_004` logits + metadata priors + texture-aware smoothing | Test whether `site/hour/site-hour` priors and texture smoothing improve the native soundscape model without retraining | 0.8157 macro ROC-AUC on the same fold (`0.7796 -> 0.8157`) | 0.737 | First Kaggle validation of the native hybrid confirms real transfer from local CV to public LB: the lightweight recipe improved substantially over pure `exp_002` (`0.647 -> 0.737`), but it still remains well below the stronger `0.890` reference blend, so inference-only adaptation is not enough yet |
| exp_006 | soundscape_finetuning_v2 | completed_first_3_folds | exp_004 | EfficientNet-B0 + GeM + attention SED head | Strengthen the native soundscape branch with non-zero `secondary_labels` weighting, a slightly larger replay bank, fold-specific outputs, and exported validation predictions for later OOF analysis | 0.7725 to 0.8313 macro ROC-AUC across folds 0-2, mean `0.7945` | n/a | Three folds are now available. The recipe is no longer a pure no-gain result, but it is still mixed: fold 1 was strong (`0.8313`), fold 0 matched the old baseline, and fold 2 dropped to `0.7725` while covering more classes (`35`). The export pipeline is working and the next question is whether priors on top of `exp_006` exports are stronger than the old `exp_004 + exp_005` branch |
| exp_007 | native_priors_on_exp006_oof | completed | exp_006 | `exp_006` exported OOF predictions + metadata priors + texture-aware smoothing | Apply the proven `exp_005` inference layer to fold-safe `exp_006` exports and compare raw-vs-postprocessed results on a more honest multi-fold OOF view | 0.7109 OOF macro ROC-AUC on folds 0-2 (`0.6646 -> 0.7109`); fold-mean best `0.8386` | 0.758 | Positive result both locally and on Kaggle. The 3-fold native hybrid beat the earlier native public result (`0.737 -> 0.758`), confirming that `exp_006 + priors` is a real improvement, but the uplift is still too small to challenge the `0.890` reference blend. This points to model/training changes, not more small postprocess tweaks, as the next main source of gains |
| exp_008 | long_context_native_sed | completed_first_3_folds | exp_007 | EfficientNet-B0 backbone + long-context temporal SED head | Replace independent `5s` clip classification with `20s` waveform contexts that emit `4` aligned `5s` windows and are aggregated back to row-level predictions with overlap-aware averaging | 0.8095 to 0.8377 macro ROC-AUC across folds 0-2, mean `0.8232` | n/a | The local branch now looks materially stronger and more stable than `exp_006` (`0.7945` mean), but the first single-fold leaderboard check through `exp_008b` still failed (`0.707`). Interpretation: long context is a real local modeling gain, yet current single-fold local validation is still too weak a proxy for promotion to the default Kaggle path |
| exp_008b | long_context_priors_postproc | completed | exp_008 | `exp_008` fold 0 exports + metadata priors + texture-aware smoothing | Apply the proven `exp_007` postprocessing layer on top of the first long-context fold to see whether the stronger native branch still benefits from priors | 0.8435 macro ROC-AUC on fold 0 (`0.8377 -> 0.8435`) | 0.707 | Negative leaderboard check. The local fold gain did not transfer: the first long-context submission underperformed both `exp_007` (`0.758`) and `exp_005` (`0.737`). The likely explanation is that the single sparse fold was not representative enough, so long-context should not become the default native submit path without more folds or a stronger validation protocol |
| exp_008c | long_context_priors_on_oof | completed | exp_008 | `exp_008` exported OOF predictions + metadata priors + texture-aware smoothing | Apply the `exp_008b` postprocessing recipe fold-safely across `exp_008` folds `0-2` and compare pooled OOF against the existing short-context native baseline | 0.7005 pooled OOF macro ROC-AUC on folds 0-2 (`0.6682 -> 0.7005`) | n/a | Important negative decision-point. Honest pooled OOF does not beat `exp_007` (`0.7109`), which explains why the single-fold long-context Kaggle attempt underperformed. `exp_008` remains a useful research branch, but it is not the next promoted submission path |
| exp_009 | noisy_student_pseudolabel | completed_3_folds_and_first_kaggle | exp_007 | EfficientNet-B0 + GeM + attention SED head student with `exp_006` fold teachers | Start the first repository-native noisy-student branch: generate fold-safe target-domain pseudo labels on unlabeled `train_soundscapes`, denoise them with power transform, and train a student with labeled soundscapes + replay + confidence-weighted pseudo sampling | fold `0`: `0.8495`; fold `1`: `0.8769`; fold `2`: `0.8849`; mean over folds `0-2`: `0.8704`; pseudo caches rows/files: `69593/9552`, `69640/9569`, `70142/9669` | 0.735 | Strong negative transfer result. Despite the best three-fold local picture among native branches, the first raw Kaggle submission underperformed both `exp_007` (`0.758`) and even the earlier postprocessed `exp_005` (`0.737`). The follow-up pooled OOF analysis in `exp_009c` confirmed that the branch remains useful research signal, but not yet a promoted submit path |
| exp_009b | noisy_student_priors_postproc | completed | exp_009 | `exp_009` fold `0` exports + metadata priors + texture-aware smoothing | Test whether the old native inference layer still helps after noisy-student training | raw remains best on fold `0`: `0.8495`; all prior variants regress (`0.7964` to `0.8112`) | n/a | Important positive-negative result: priors are no longer helping and instead hurt strongly. This suggests the noisy-student branch is learning more of the metadata/texture structure internally, so `exp_009` should be treated as a raw-model branch first rather than automatically inheriting `exp_007` postprocessing |
| exp_009c | noisy_student_pooled_oof_analysis | completed | exp_009 | `exp_009` fold exports + pooled OOF / calibration diagnostics | Build the honest pooled OOF view for `exp_009`, measure fold-mean optimism, and test whether lightweight context repair or temperature-style calibration rescues the branch before another Kaggle attempt | raw pooled OOF macro ROC-AUC `0.7934`; raw fold-mean `0.8704`; optimism gap `0.0770`; raw remains best fixed variant; fold-safe selection drops to `0.7853` | n/a | Critical analysis result. The branch is locally real, but its simple summary was overly optimistic. More importantly, no lightweight postprocess or calibration rescue improved pooled OOF over raw. That means the next move should be a new modeling branch, not another inference-only tweak on top of `exp_009` |
| exp_011 | hgnetv2_soundscape_supervised | completed_4_folds_and_second_kaggle | 0.856 HGNetV2 reference | `hgnetv2_b0.ssld_stage2_ft_in1k` | Rebuild the strong HGNet supervised recipe locally: unified `train_audio + labeled soundscape clips`, grouped multi-label CV, optional wav-cache, and soundscape-aware validation exports | folds `0-3`: soundscape-only macro ROC-AUC `0.8509 / 0.8042 / 0.8544 / 0.7992`, mean `0.8272`; overall macro ROC-AUC mean `0.9562` | 0.850 | Second Kaggle submission improved the branch again (`0.844 -> 0.850`). This validates the fold-expansion idea, but only with a modest gain, which suggests that `exp_011` is now a very strong native baseline with diminishing returns from more pure fold scaling. The next highest-value move is to compare this branch against `exp_012` rather than only adding more HGNet folds |
| exp_012 | perch_temporal_light | completed_first_oof_negative | 0.924 Pantanal Distill / ProtoSSM reference | `Perch v2 embeddings + ProtoTemporalLight (bi-SSM + prototypes + site/hour embeddings + gated fusion)` | Build a lighter first-pass temporal downstream model on cached `perch_meta` file sequences before attempting the full ProtoSSM stack | grouped OOF macro ROC-AUC `0.6248`; raw Perch on the same cached files `0.7390`; fold AUCs `0.8337 / 0.6862 / 0.8705` | n/a | Important negative result. The first light temporal model underperformed raw Perch by `-0.1142` on pooled OOF despite decent-looking per-fold scores, which means the current architecture is unstable and the fold summary is too optimistic. This branch is still valuable, but it now needs simplification/ablation before any Kaggle promotion |
| exp_012b | perch_temporal_ablation | completed_first_oof_negative | exp_012 | raw Perch + pooled MLP + simplified SSM variants | Build a cleaner ablation ladder over cached `perch_meta` to isolate whether the failure in `exp_012` comes from the temporal block itself or from the prototype/gated-fusion stack | grouped OOF macro ROC-AUC: `raw_perch 0.7390`, `pooled_mlp_rawfeat 0.6176`, `ssm_linear 0.3355`, `ssm_linear_rawfeat 0.4286` | n/a | Strong negative control. Raw Perch remained best and every learned variant regressed, which means the immediate problem is not only the prototype/gated-fusion complexity from `exp_012`. On the current trusted `59` files and grouped split, even much simpler Perch downstream models are not yet stable improvements. This sharply raises the value of the native `exp_011` path as the main optimization track |
| exp_014 | hgnetv2_pseudolabel | completed_4_folds_mixed_negative | exp_011 | `hgnetv2_b0.ssld_stage2_ft_in1k` student + `exp_011` 4-fold teachers | Continue the strongest native branch with fold-safe pseudo labels on unlabeled `train_soundscapes`, keeping the same HGNetV2 frontend/backbone and soundscape-aware checkpoint selection | folds `0-3`: soundscape-only macro ROC-AUC `0.8684 / 0.8154 / 0.8301 / 0.7702`, mean `0.8210`; pseudo caches `20688/5471`, `22188/5646`, `20211/5333`, `20595/5293` | n/a | Final four-fold result is negative relative to the supervised parent branch. `exp_014` wins on folds `0-1` but loses clearly on folds `2-3`, and the mean falls below `exp_011` (`0.8210` vs `0.8272`). This makes the branch valuable as a research finding, but not a promoted submit path |
| exp_014b | hgnetv2_pseudolabel_strict | completed_first_fold | exp_014 | `hgnetv2_b0.ssld_stage2_ft_in1k` student + `exp_011` 4-fold teachers | Test a stricter pseudo-label recipe: higher confidence threshold, lower pseudo weight, fewer pseudo windows per file, global pseudo cap, and delayed pseudo usage starting from epoch `2` | fold `0`: soundscape-only macro ROC-AUC `0.8683` at epoch `4 / 6`; pseudo cache `8724` rows across `3237` files with mean confidence `0.6358` | n/a | Strong diagnostic win. `exp_014b` matches `exp_014` on fold `0` almost exactly (`-0.00019`) while using a much smaller and cleaner pseudo cache. That does not yet make it a new submit path, but it strongly supports the hypothesis that `exp_014` failed because the pseudo recipe was too aggressive rather than because pseudo labels are useless |
| exp_015 | pantanal_proto_ssm_v17_submit_path | completed_first_kaggle | 0.924 Pantanal Distill / ProtoSSM reference | faithful external Perch + ProtoSSM v5 + probe + residual SSM submit stack | Operationalize the strongest known external notebook as a portable Kaggle submission path with dynamic input resolution instead of another local simplification | n/a | 0.925 | Major success. The faithful external operationalization not only worked, it slightly exceeded the reported `0.924` reference and became the new best public result in the repository. This strongly supports the conclusion from `exp_012` / `exp_012b`: the high external ceiling is real, but it must be preserved almost intact rather than approximated through light local Perch ablations |
| exp_015c | full_v18_submit_path | attempted_direct_submit_timeout | exp_015 + 0.927 Pantanal V18 reference | larger ProtoSSM + larger residual + updated probe/postprocess settings in one monolithic submit notebook | Test the full V18 upgrade as a direct Kaggle submit path without first artifactizing the downstream components | n/a | timeout | Useful negative engineering result. Even with `P100`, the notebook still timed out because submit mode continued to do heavy train-time work (`ProtoSSM`, probes, and sometimes residual correction training). This suggests the V18 path should be split into offline export and thin submit instead of being optimized further as a single notebook |
| exp_015d | v18_artifactized_submit_path | completed_first_kaggle | exp_015c | offline artifact export + thin Kaggle submit | Preserve the V18 quality hypothesis while removing submit-time training by precomputing and packaging downstream artifacts (`ProtoSSM`, probes, residual, priors, thresholds) | n/a | 0.929 | Major engineering and modeling win. Artifactization preserved the V18 stack, removed the submit-time timeout bottleneck, and improved the project best public score from `0.925` to `0.929`. This confirms that the V18 gains were real, and that the earlier failure mode was operational rather than conceptual |
| exp_016 | blend_exp015_exp011 | historical_scaffold_superseded | exp_015 + exp_011 | weighted blend of two existing `submission.csv` files | First complementarity scaffold after `exp_015 = 0.925`; later abandoned because Kaggle-downloaded CSV artifacts were not reliable enough for direct row-aligned blending | n/a | n/a | Useful historical branch. It established the blend question and produced the reusable runtime `exp_011` inference block, but the original CSV-based plan became obsolete once `exp_015d = 0.929` replaced `exp_015` as the main external path |
| exp_016b | blend_exp015d_exp011 | completed_first_kaggle_no_gain | exp_015d + exp_011 | artifactized V18 submit path plus runtime HGNetV2 blend | Test whether the strongest overall path (`exp_015d = 0.929`) still gains from a small runtime blend with the strongest repository-native branch (`exp_011 = 0.850`) without disturbing the timeout-safe artifactized V18 route | n/a | 0.929 | Clean no-gain result. After trimming the native add-on to the strongest `exp_011` folds and adding runtime guards, the blend matched `exp_015d` exactly instead of improving it. This suggests that simple generic complementarity from `exp_011` is weak at the current score level, so future blend work should be optional rather than a top priority |
| exp_017 | v18_error_report | completed | exp_015d and exp_011 diagnostics | pooled native error analysis notebook | Build a targeted diagnostic report by taxon, site, hour, and class difficulty so future improvements attack a concrete weak regime instead of another generic blend | pooled `exp_011` macro AUC `0.9468`; soundscape-only `0.7612`; weakest taxa `Amphibia`, then `Insecta` | n/a | Useful direction-setting branch. It showed clearly that the next modeling step should target texture-heavy taxa rather than add another generic ensemble |
| exp_018a | texture_specialist_oof | completed_4_folds_positive | exp_011 | `hgnetv2_b0.ssld_stage2_ft_in1k` specialist trained only on `Amphibia + Insecta` target rows | Test whether the native weakness on texture-heavy taxa is concentrated enough to justify a dedicated specialist model | target soundscape macro AUC over folds `0-3`: `0.8056 / 0.8254 / 0.7914 / 0.8004`; four-fold mean `0.8057`; same-taxa generic `exp_011` mean `0.7874` | n/a | Modest but real positive signal. The specialist is not stable enough to replace the generic branch globally, but it is strong enough to justify a correction-layer interpretation |
| exp_018b | targeted_merge_benchmark | completed | exp_018a + exp_011 | pooled aligned OOF targeted merge benchmark | Benchmark soft replacement of only `Amphibia + Insecta` columns to see whether a targeted correction branch beats both the generic and pure specialist extremes | generic target soundscape macro AUC `0.7218`; specialist `0.7450`; best tested targeted blend `w_spec = 0.75` gives `0.7465` | n/a | Strong local justification for a Kaggle overlay branch. The best result is a soft targeted merge, not a hard overwrite |
| exp_018c | exp015d_texture_overlay | scaffolded_for_first_kaggle | exp_015d + exp_018a | `exp_015d` V18 artifactized submit path with targeted HGNetV2 overlay for `Amphibia + Insecta` only | Keep the strongest external submit intact while blending in a conservative specialist correction only on the weak texture-heavy taxa | local design anchor from `exp_018b`: target-only best weight `0.75` on `exp_011` proxy; first Kaggle config intentionally reduced to `0.35` with folds `(0, 1)` | pending | First Kaggle-facing targeted overlay path. Packaged assets live in `submissions/kaggle_datasets/birdclef-exp018a-texture-specialist-4fold`, and the submit notebook is `notebooks/kaggle_submission_exp_018c_exp015d_texture_overlay.ipynb` |
